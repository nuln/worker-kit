# S3 Multi-Cloud Storage Module (`@nuln/worker-kit/s3`)

[English](./s3.md) | [简体中文](../zh/s3.md)

The S3 package provides a native, zero-dependency AWS SigV4 signed client compatible with all major S3-compliant cloud object storage providers.

## Supported Providers

- **Cloudflare R2** (`provider: "r2"`)
- **Amazon Web Services S3** (`provider: "aws"`)
- **MinIO & Local S3** (`provider: "minio"`)
- **Wasabi Hot Cloud Storage** (`provider: "wasabi"`)
- **Backblaze B2** (`provider: "b2"`)
- **Alibaba Cloud OSS** (`provider: "oss"`)
- **Tencent Cloud COS** (`provider: "cos"`)

## Basic Usage

```ts
import { createS3Client } from "@nuln/worker-kit";

const s3 = createS3Client({
  provider: "r2",
  endpoint: "https://<account-id>.r2.cloudflarestorage.com",
  accessKeyId: env.R2_ACCESS_KEY_ID,
  secretAccessKey: env.R2_SECRET_ACCESS_KEY,
  bucket: "production-bucket",
});

// Upload
await s3.putObject("logs/2026-09-17.json", JSON.stringify({ event: "boot" }), {
  contentType: "application/json",
});

// Download
const response = await s3.getObject("logs/2026-09-17.json");
const data = await response.json();

// Presigned URL
const downloadUrl = await s3.getPresignedUrl("logs/2026-09-17.json", {
  method: "GET",
  expiresIn: 3600,
});
```
